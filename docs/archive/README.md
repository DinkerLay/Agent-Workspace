# Historical material removed

Historical specifications, research, plans, mockups, and generated assets were deliberately
removed from this working tree during the direct Runtime cutover. They are not implementation
inputs, compatibility material, or a second documentation authority.

Use VCS history to recover a specific historical revision when needed. Do not copy historical
payloads back into `docs/archive/`; keep current product truth in `../architecture.md` and the
only executable plan in `../implementation-plan.md`.
