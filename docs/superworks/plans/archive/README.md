# 计划归档

这里的文件只保存实现记录和决策证据，不是当前工作指令。

- `completed/`：已完成并由当前 spec 吸收的计划。
- `superseded/`：被后续规范或计划替代的方案。
- `audits/`：某一时间点的审计和缺陷清单；其中的“当前”只代表审计日期。
- `fixtures/`：历史生成样例，不是可导入的当前 Template。

当前计划从 [`../README.md`](../README.md) 进入。
